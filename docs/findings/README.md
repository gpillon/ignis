# Findings

This collection preserves durable, reusable, evidence-backed results that can
inform future work in this repository. To add, update, or supersede a finding,
follow the [authoring convention](../agents/findings.md).

## Classification

- `discovery`: durable observations from exploring the codebase, architecture,
  runtime, or workflow.
- `research`: conclusions drawn from identifiable external sources.
- `experiment`: results from reproducible tests, benchmarks, or measurements.

## Status

- `current`: the finding remains usable.
- `superseded`: the finding is retained as historical context and links to the
  material that replaces it.

## Index

| Finding | Kind | Scope | Observed | Status | Superseded by | Summary |
|---|---|---|---|---|---|---|
| [hq-e8-2b KV capacity](2026-09-11-hq-e8-2b-kv-capacity.md) | discovery | kernel / paged KV cache, scheduler capacity | 2026-09-11 | current | none | A sequence-token costs 9,216 bytes under hq-e8-2b against 65,536 under BF16 (7.11x), so 8 lanes at 40,960 context need ~3.02 GB instead of 20 GiB. |
| [Prefill chunk wall time](2026-09-11-prefill-chunk-wall-time.md) | experiment | kernel / prefill chunking, GPU profiling | 2026-09-11 | current | none | A 1,024-token prefill chunk is 90% per-layer compute and 0.5% the forced synchronization, so #92's "8 semaphores" premise does not hold; packing helps only below ~1,024 tokens/traversal by filling GEMM shape and amortizing launch overhead, not by removing syncs. |
| [Sequence snapshot transfer cost](2026-09-12-sequence-snapshot-transfer-cost.md) | experiment | kernel / sequence state transfer, KV-RAM host tier | 2026-09-12 | current | none | Snapshot and restore both run at ~12 GB/s because this host caps the 5090 at PCIe Gen 3 x16, so a full-context round trip costs ~90 ms rather than ADR 0024's estimated ~42 ms — still around 100x cheaper than the re-prefill it replaces; the DFlash2 drafter's window and checkpoint add exactly 80 MiB and ~7 ms per direction (2026-09-14). |
| [hq attention route agreement](2026-09-12-hq-attention-route-agreement.md) | experiment | kernel / GQA attention routes, hq-e8-2b KV cache | 2026-09-12 | current | none | hq attention agrees with ignis's own BF16 route to ~0.23 median relative L2 (cosine > 0.95) on identical keys and values, and its handful of rows past 1.0 are softmax flips between near-tied keys — so the enforced bound is the median plus a capped fraction, never a per-row maximum. |
| [Device prefix clone cost](2026-09-12-device-prefix-clone-cost.md) | experiment | kernel / device prefix reuse, sequence state transfer | 2026-09-12 | current | none | The device-to-device clone behind prefix reuse costs ~0.25 ms for 148 MiB of mutable state, 2.8x ADR 0024's estimate but 96x cheaper than a PCIe round trip and ~8,000x cheaper than re-prefilling the head; issuing it as one strided copy per section instead of one per layer is worth 6x. |
| [hq prompt workspace under-report](2026-09-12-hq-prompt-workspace-under-report.md) | discovery | kernel / vendored GQA workspace query, hq-e8-2b prefill | 2026-09-12 | current | none | The vendored capacity query under-reports an hq-e8-2b prompt call by one split-partial set at widths 9..16, so a short prompt overruns its arena; ignis corrects it at the caller rather than patching the vendored source. |
| [GPU profile wall time](2026-09-12-gpu-profile-wall-time.md) | experiment | test harness / GPU profile, f64 layer oracle, artifact materialization | 2026-09-12 | current | none | The profile's dominant host cost was an unoptimized single-threaded f64 oracle (123.94 s to 3.86 s once optimized and threaded, bit-identical); what remains is one ~8.4 s artifact upload per test binary, ~170 s of a 564 s stage, too small to buy the isolation a single-process harness would cost. |
| [ITL lane terminator race](2026-09-13-itl-lane-terminator-race.md) | discovery | bench / G3 ITL cell, EOS suppression, reference engine capabilities | 2026-09-13 | current | none | A decode lane can be ended by the harness, its own EOS or the safety cap and the fixture pins none of them — the reference honours neither `ignore_eos` nor `logit_bias`, so measuring inside the span every lane shared (6-8 of 10 prefill windows on the three refused records, p95 within 0.7% of the accepted leg) keeps the cell honest where the old outlive-the-series guard refused the leg. |
| [hq vs BF16 decode cost](2026-09-13-hq-vs-bf16-decode-cost.md) | experiment | runtime / KV format, decode throughput, GPU utilization | 2026-09-13 | current | none | BF16 beats hq-e8-2b by ~12% on C=1 and ~13% on ITL p95 on **both** engines, so that cost is the format's rather than ignis's; what is ignis's is memory idleness — at the reference's power it reaches 25-27% memory utilization against ninfer's 37-44% — while at C=4 under hq ignis is 2.7x the reference. |
| [G4 zero-token main was the reader](2026-09-13-g4-zero-token-main-was-the-reader.md) | experiment | bench / G4 per-class cell, trace replay, SSE reader | 2026-09-13 | current | none | The G4 run's `main` request did not return nothing — ignis generated 16,000 thinking tokens — but its records came from an `ignis-bench` built before #137's reasoning-channel fix; replayed with the current reader it counts 15,613 tokens on ignis and 16,000 on ninfer under the trace's load, so the cell is a measurement once the gate is re-run. |
| [hq vs BF16 live/live](2026-09-13-hq-vs-bf16-live-live.md) | experiment | runtime / KV format, decode throughput, GPU utilization | 2026-09-13 | current | none | Re-measured as a gate measures — one session, two process launches per (engine, format) cell — hq-e8-2b costs **14.5%** of ITL p95 on ignis and **14.3%** on the reference, so the penalty is the format's under live/live discipline too; at matched format ignis is 1.06/1.02 on C=1 and 3.07/1.94 on C=4, and behind by the same 1.03 on ITL p95 in *both* formats. |
| [GPU profile fits one run](2026-09-13-gpu-profile-card-fits-one-run.md) | experiment | test harness / GPU profile, GPU exclusivity | 2026-09-13 | current | none | The serialized sweep is green 3 of 3 on an exclusive card with no memory drift between runs, but one run peaks at 26.1 of the 5090's 32.6 GiB — too little headroom for a second GPU-touching process, whose loser dies with `exit code: 1` and no panic text, which is what #145's two unrelated crashes look like. |
| [Batched decode width drift](2026-09-14-batched-decode-width-drift.md) | experiment | kernel / window-0 batched decode, batch invariance | 2026-09-14 | current | none | Greedy batched decode at M=4 picks a token 0.75–0.875 logits behind (by both prefill routes) where M=1/M=2 and every verify round agree, so spec-off is itself a drifting route at width > 2 and cannot serve as bit-exact ground truth. |
| [Decode rope delta token insensitivity](2026-09-16-decode-rope-delta-token-insensitivity.md) | experiment | kernel / multimodal decode rotation, GPU test design | 2026-09-16 | current | none | Shifting the decode rotation by -24, 0 or +400 positions left 40 greedy tokens identical on qwen3.8-27B, and only +200,000 changed them (after ~12 tokens), so a token-equality GPU test at a real image's delta cannot detect a dropped `rope_delta`. |
| [Vision TTFT live/live](2026-09-16-vision-ttft-live-live.md) | experiment | serving / multimodal TTFT, text TTFT with `--vision` loaded, VRAM headroom | 2026-09-16 | current | none | Pooled over two launches per engine, ignis's screenshot TTFT is 1.24x the reference's but its vision part costs 21 ms against the reference's 63 ms (the ratio is the short-prompt text gap, 1.85x at 1,024 tokens); loading `--vision` leaves text TTFT at 0.95/1.10/1.03x, and a discarded session was 2-5x slow from VRAM oversubscription by the desktop. |
| [VRAM budget holds memory flat](2026-09-18-vram-budget-flat-memory-gate.md) | experiment | serving / device memory reservation, KV-RAM pinned arena, retained slots, WDDM paging | 2026-09-18 | current | none | With #207's slices in, the load that grew the process 2,734 MiB in 14 minutes now grows it 6 MiB in 24.5, process shared sits on the 8 GiB pinned KV-RAM arena and never moves, no sample trades dedicated for shared at constant commit (23 before), and the printed `vram_plan` total matches Task Manager's dedicated figure to 16 MiB. |
| [GQA workspace memset is dead work under hq](2026-09-18-gqa-workspace-memset.md) | experiment | kernel / GQA attention workspace, prefill chunk cost, GPU A/B method | 2026-09-18 | current | none | Under hq-e8-2b every byte A1 reads is written by the launch in front of it, so the per-layer workspace zeroing was dead work worth 2.12% of GQA layer device time on a 70K prefill (~162 GB of writes removed); BF16 keeps it, and the effect is only resolvable per-layer against the untouched GDN layers as an in-run control. |
| [Decode round host idle](2026-09-18-decode-round-host-idle.md) | experiment | kernel / decode round, CUDA graph replay, device timeline | 2026-09-18 | current | none | The 224 memcpy/memset nodes per decode round cost only their own execution — the device resumes 0.10 us after a residual copy retires — while a round leaves the device idle ~1 ms in five host-side stalls that are fixed per round, 4.91% of the timeline at one lane and 2.27% at eight, the largest 389-406 us of it between the last staging copy and the graph launch. |
| [Drafter top-k, one warp per column](2026-09-18-dflash2-topk-one-warp-per-column.md) | experiment | kernel / DFlash2 drafter, speculative decode round, vendored op replacement | 2026-09-18 | current | none | The vendored `dflash2_topk` gave one warp to each of seven columns over a 248k vocabulary and spent 3,145 us — 16.4% of decode kernel time — doing 2.0 us of memory traffic; a row-split replacement of ours does it in 44 us at a bit-identical answer, worth 17.6% of a decode round and +21% of decode throughput at one lane, 4.1% at eight. |
| [Decode round anatomy](2026-09-18-decode-round-anatomy.md) | experiment | kernel / decode round, CUDA graph submission cost, roofline accounting | 2026-09-18 | current | none | A decode round is 1,166 graph nodes and 15.81 ms of device time that is weight streaming at the card's bandwidth (backbone GEMM at 100% of roofline, output head at 86%); `cudaGraphLaunch` costs 0.36-0.73 us per node so submission is 2.2% of the round, and decode's whole remaining headroom is the 4.8% it spends idle, which is structural — two host syncs per round — not per-kernel. |
| [Browser connection limit caps parallel streams](2026-09-18-browser-connection-limit-caps-parallel-streams.md) | experiment | Playground transport / parallel agents, Monitor scraping, anything the page requests while streams run | 2026-09-18 | current | none | Six agent streams take all six connections a browser opens per origin over HTTP/1.1, so the Monitor scrape and every other same-origin request sit queued until an agent ends, and two of the engine's eight lanes are unreachable from the page; h2/h3 multiplex and lift the limit. |
| [Prompt-reuse tax on short TTFT](2026-09-18-prompt-reuse-tax-on-short-ttft.md) | experiment | serving / prefill chunking, cross-request state reuse, the G2 TTFT cell | 2026-09-18 | current | none | Cross-request reuse cuts a prompt at its publish point and again at the generation opener, turning one model traversal into three; at 1,024 tokens that is 74.7 ms of extra TTFT (204.3 to 129.6 ms with `--prompt-reuse off`), because a traversal costs ~19 ms fixed whatever its token count — invisible on a long prompt, 37% on a short one, and the G2 cell is cold-prefix by construction so the bet can never pay there. |
| [YaRN needle at 320K is not discriminating](2026-09-19-yarn-needle-320k-not-discriminating.md) | experiment | serving / RoPE scaling, long-context evaluation method | 2026-09-19 | current | none | A literal needle at 320,076 tokens (1.22x the trained envelope) is recalled exactly both with `--rope-scaling yarn:4` and without it, so the obvious long-context probe would also have passed a build that ignored the flag; what discriminates is a question about relative position, not one string. |
| [Typed option logits are readable from one prefill](2026-09-19-typed-option-logit-readout.md) | experiment | serving / classification readout, prefill logits, `Compute` seam | 2026-09-19 | current | none | On the served 27B the unrestricted next-token winner is a declared option letter on 144/144 authored decisions and the declared options hold a median 99.8% of the distribution, so a Jev-shaped readout is signal rather than renormalized noise: 0.934 balanced accuracy, 36.5 ms and zero decoded tokens per decision, and every one of the 118 rows above 0.9 top probability is correct; the mass does not decay out to 256 declared options (in-slot 100% at every width), and the readout holds over an image on all four vision canaries. |
| [A constrained digit readout points at a button](2026-09-19-constrained-digit-readout-points.md) | experiment | serving / constrained decode, multimodal grounding, decision endpoint primitives | 2026-09-20 | current | none | One prefill plus six digit-constrained steps reads a click target off a 4096px screenshot to within 84 px worst case, inside the button on all three scenes — reproduced end to end through `POST /v1/decide`, on a drafter load as well; the model answers on a 0-999 normalized scale, the constraint reproduces its own free answer to within (0,9), and the per-digit probability falls from ~0.98 to ~0.15 across each number, which is the model reporting its own spatial resolution. |
| ["Evidence first" was never in the bytes](2026-09-20-evidence-first-needs-explicit-key-order.md) | discovery | serving / decisions, the `/v1/decide` prompt, shared-prefix reuse | 2026-09-20 | current | none | `serde_json::Map` sorts its keys in this workspace, so every measured decision prompt serialized as `{"criterion":…,"evidence":…}` — the question in front of the evidence — and two questions over one `state` shared nine characters rather than the whole evidence; ADR 0034's reuse claim and spec 04's fan-out acceptances were false of the prompt as built, while the accuracy numbers stand but describe a criterion-first prompt that `/v1/decide` no longer sends. |
| [The evidence belongs in the system block](2026-09-20-the-evidence-belongs-in-the-system-block.md) | discovery | serving / decisions, the `/v1/decide` prompt, cross-request state reuse, spec 04's fan-out | 2026-09-20 | current | none | A decision is never a live publisher and captures no checkpoint, so the only tier a sibling question can claim is a retained prefix — cut at the page floor of `system_block_tokens` and never reaching outside it — which makes evidence at the head of the user payload first, shared and re-prefilled N times; moved into the system block a realistic state retains 384 of its 388-token block (each sibling re-prefills 72 tokens of 456), the 144-row sweep of the new layout beats the measured one at 0.963 balanced accuracy against 0.934 with the mass unchanged at 0.999, and a state under one page — Jev's own documented example — still shares nothing. |
| [A fixed-width `number` is wrong at exactly one width](2026-09-20-number-width-and-decide-e2e.md) (width rule corrected 2026-09-21) | experiment | serving / `POST /v1/decide`, constrained decode, fan-out cost | 2026-09-20 | current | none | On a live server the `number` primitive answers correctly when `digits` equals the value's own width or exceeds it by two or more, and is wrong by a factor of ten at exactly one more — 7 to 70, 42 to 420, 230 to 2300 — because the model writes left-aligned and fills the last step with a zero; the first digit's probability falls to 0.65-0.71 in that case against 0.94-1.00 when the width fits, so the trace flags it. `box` lands within 6 px on x and 30-80 px on y of the ground truth on all three pointing scenes, which #242 shipped as unmeasured. A fan-out over an **image** state shares nothing and costs 6.9 s per question exactly linearly, against 33 ms marginal for a text state in the system block; the split says the bigger half is not the prefill but the **vision encode** — 4.17 s of tower per question over bytes the media cache has already prepared, against ~2.4 s of attention prefill — so a shared-prefix fix for GitHub #240 would recover the smaller half and leave the embedding re-encoded N times. |
| [A shared vision embedding turns a 4x fan-out into 2.1x](2026-09-20-shared-vision-embedding-fan-out.md) | experiment | serving / multimodal fan-out, vision encode cost, device memory reservation | 2026-09-20 | current | none | Keying the encoder output by `(content digest, grid)` and holding it past its request turns 1/2/4 questions over one 4096x4096 screenshot from 6.84/13.50/27.92 s into 6.27/8.46/13.24 s on one encode instead of four (9.49 s when the picture was already seen), with the `media_embedding` plan line unmoved at 320 MiB. |
| [The vision tower is quadratic because the checkpoint is](2026-09-21-vision-tower-cost-at-width.md) | experiment | serving / vision encode | 2026-09-21 | current | none | Full attention over every patch of a still image is the served checkpoint's scheme — ignis, ninfer's C++ control and its Python reference build the same one-segment-per-frame `cu_seqlens`, the config declares no windowing, and the kernel is byte-identical — so the curve is the scheme, not a bug: the profiler puts 88.2% of a 16,384-column encode in the attention kernel, which scales x50.69 against an exact quadratic x50.57 and holds the same 164.7 TFLOP/s at both widths with 0.1% device idle, and a two-term fit over six un-profiled widths reproduces that split to within 0.5%. 16,384 columns is the processor's own ceiling (`max_pixels` = 4096²), so 3.67 s is the worst case per image and the only lever is sending a smaller one — pointing lands inside the button at every size down to 576 columns (180x cheaper) and `box` is in fact *most* accurate at 2048 px; `--vision-max-tokens` refuses images rather than shrinking them (#248). |
| [The number prompt declared a width and not an alignment](2026-09-21-the-number-prompt-declares-an-alignment.md) | experiment | server / `/v1/decide` constrained decode, prompt | 2026-09-21 | current | none | `number` answered 300 for a truth of 3 because the schedule forces exactly K digits with no way to say a number is finished and `number_system` declared the width without naming which end to pad, so the model chose an alignment at the first digit — `0` at p = 0.48-0.91 for right, the value's own leading digit at p >= 0.93 for left — and 10 of the 21 widths that could hold the value came back multiplied by a power of ten. One clause in the prompt ("right-aligned and padded on the left with zeros") takes it to 21 of 21 on truths of one, two and three digits, leaving only widths narrower than the value, which can only truncate. This corrects the earlier finding's rule that two digits of headroom are safe: 47 is right at four digits and 4700 at five. |
| [A number that closes its own object needs no width from its caller](2026-09-21-a-number-that-closes-its-own-object.md) | experiment | server / `/v1/decide` constrained decode, engine / schedule stopping condition | 2026-09-21 | current | none | Permitting the closing `}` from the second step on lets a constrained decode end when its answer is complete: on six evidence-grounded truths with **no `digits` named by the caller** the model closed every run itself, read every value exactly — including `2.5` and `-0.75`, which a fixed field cannot express — and spent 2 to 6 rounds instead of a fixed 6. The fixed field was never load-bearing; the caller's guess at a magnitude, #254's alignment clause and the padding excluded from its uncertainty were all consequences of a schedule having no way to say "done". The cost is that a short run stops being self-evidently a fault: three outcomes, told apart by the last token and never by the length, and only one is an error. A static schedule still cannot say "at most one decimal point", so the alphabet permits and the reader refuses. |
| [A scalar reads its legend, and only while the legend is narrow](2026-09-21-scalar-readout-legend-width.md) | experiment | server / `/v1/decide` readout primitives, prompt width | 2026-09-21 | current | none | A grid of ordered cells named by single-token labels reads a scalar in one prefill with zero decoded tokens, and a permuted legend proves the model reads the legend rather than the slot (four LEGEND, zero SLOT at 100 cells; six of six at 20) — but accuracy is bounded by the legend's *width*, holding 4/5 at 20 and 32 cells and collapsing to 2/6 at 48 and 0/6 at 100, and it collapses on the rows that need computation before the lookup while direct lookups survive 100 cells at p >= 0.90. Coarsening a range that cannot be narrowed recovers it (0-100 at step 5 moves a percentage from noise to within one cell). The argmax cell beats the probability-weighted mean `score` returns on every set measured (0.00 against 1.24 median error), and `number` remains the more accurate primitive — what the readout buys is zero decoded tokens, no lane and no width trap, not precision. |
| [The grid a bigram can name](2026-09-21-the-grid-a-bigram-can-name.md) | experiment | serving / decision readout primitives, answer alphabet, one-pass pointing | 2026-09-21 | current | none | 114 of the 676 uppercase bigrams are not single tokens and the holes cluster in the second half of the alphabet, so the largest **contiguous** grid a compositional `row+column` label can name is 9 x 9 (11.11% of the side per cell) and the largest non-contiguous one 18 x 18 (5.56%) — both far coarser than the 2.1% the digit chain already reaches, and an argmax cell misses `small`'s button by construction. Single uppercase letters have no holes (26 of 26), so a per-axis **strip** readout is nameable where a 2-D cell is not: the resolution is in two positions, not in one label. |
| [The point is assembled as it is written](2026-09-21-the-point-is-assembled-as-it-is-written.md) | experiment | serving / decision readout primitives, one-pass pointing | 2026-09-21 | current | none | At the position where the model is about to write x's first digit the latent holds x to about one digit (20.9 units against a leading-digit baseline of 25.0) and holds y barely at all, so the number is not waiting to be serialized: a cross-validated probe on the residual lands inside the button on 97 of 240 scenes (111 with an RBF kernel) where the chain itself lands 218, and the learning curve is flat from n=120. Since a fitted probe is strictly stronger than a fixed readout into the vocabulary, this bounds **every** one-position reading of that position, not just the probe. The probe's accuracy climbs as the chain writes - 97, 143 once x is spelled, 187 - so each forced digit decides the number rather than reporting it. Two method lessons: the best probe beats the chain on mean error (2.58% against 2.75% of the side) while landing inside less than half as often, so acceptance 3 must be scored as the predicate it is; and the best site is L47, not the final residual (6.54%), which is the only place a hidden state is available today. |
