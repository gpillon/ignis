# ignis — project review, root problems, reorganization, path to a usable engine

Date: 2026-09-05. Scope: the whole repo (`crates/*`, `kernel/`, `docs/`,
`.scratch/*/specs`, `PENDING.md`, `KERNEL-WIP.md`) read against the reference
(`F:/ai/q38/ninfer`, ~92K lines C++/CUDA, Apache-2.0) and the real artifact
(`qwen3_8_27b_nvfp4full-v2.ninfer`). Evidence is cited as `file:line`.

---

## 0. Verdict (TL;DR)

1. **The engine has never produced a real completion, and it cannot on the
   current path.** `KERNEL-WIP.md` attributes this to one launch bug (#35). The
   launch bug is real, but it is the *shallowest* of at least eight
   correctness defects in the forward pass. Fixing #35 yields a forward that
   still computes the wrong function (wrong weight scales, wrong GDN gating,
   wrong RoPE positions, wrong state shape, missing norms).
2. **The compute architecture is inverted.** Activations, KV cache and GDN
   state live in host `Vec<u16>`; every op is an FFI call that H2Ds its inputs
   and D2Hs its outputs; residual/SiLU/readout are host loops. This is not a
   "for now" that gets optimized later — it is a different program. ninfer
   streams ~15.85 GB/token of weights in ~13 ms; ignis would move the
   per-request KV cache (hundreds of MB) and the GDN state (1.2 GB bf16 flat)
   over PCIe **per layer per token**. Expected gap: 2–3 orders of magnitude,
   before kernel quality.
3. **The kernels are naive scalar rewrites, not ports.** One-block-per-row
   scalar GEMV, 16×16 shared-memory scalar GEMM, decode attention that does a
   block reduce *per key* in a serial loop, a 1024-thread smem-reduced GDN
   step. ninfer's equivalents are warp-MMA / TMA / tensor-core split-KV /
   register-tiled warps, already at or near roofline. The "faithful
   adaptation" provenance comments are not accurate.
4. **The CUDA-graph "decode hot path" is not the model.** The captured graph
   is embed → *one* attention kernel → *one* GDN step → final norm → lm_head,
   over zeroed state with no layers and no projections
   (`kernel/src/decode_graph_surface.cu:19-25`). `decode_step` routes every
   batch-1 request through it (`compute.rs:3282-3287`), so the "hot path"
   would serve logits unrelated to the prompt.
5. **The Rust core (scheduler, paged KV pool, admission, host tier, prefix
   reuse) is a self-consistent CPU model that is not connected to any device
   memory.** `KvPool`/`BlockTable` are never referenced by `compute.rs`; the
   compute adapter keeps its own per-request host `kv_cache` + `block_table`.
   215 CPU tests pass; 26 GPU tests are `#[ignore]`d and self-skip. Green
   `cargo test` currently proves nothing about inference.
6. **Reorganization is needed — but not of the repo layout.** The workspace
   shape (Rust crates + C++/CUDA leaf behind a C ABI) is fine. What must
   change is the **level of the ABI** (per-step, device-resident, not per-op
   with host pointers), the **ownership of the forward pass** (a C++ program
   layer, like ninfer's `text_context_impl.h`), and the **kernel policy**
   (actually port ninfer's kernels, verbatim, with their layouts — or write
   tensor-core kernels; never scalar stand-ins).
7. **Honest distance to "at least what ninfer does":** ninfer is ~55K lines
   of ops + ~30K of runtime/targets/serve, tuned over months. Reaching its
   MTP0 decode (75 tok/s) and prefill (~8.5K tok/s @24K) with hq-e8-2b, prefix
   reuse, KV-RAM tier, MTP/DFlash2 is realistically **4–6 months** of focused
   work with agent assistance and a GPU-in-the-loop test discipline, *if* the
   kernels are ported rather than reinvented. The current process (specs →
   agent → CPU-green → "resolved") produced ~28K lines of Rust in 4 days and
   zero working inference; it must change before more code is written.

---

## 1. What exists and what actually works

| Area | Files | Status | Evidence |
|---|---|---|---|
| Artifact reader / binder / checksum / frontend (tokenizer + chat template) | `crates/artifact` (~5.6K) | **Works, verified on the real artifact.** 19 GB VRAM materialization OK. Real value, keep. | `PENDING.md` artifact-01; `real_artifact.rs` |
| HTTP server, OpenAI schemas, SSE, telemetry JSONL | `crates/server` (~2.9K) | Works against `MockCompute`. Keep. | `openai_http.rs`, `telemetry.rs` |
| Scheduler, admission state machine, paged KV pool, host tier, prefix cache | `crates/core` (excl. compute) (~3.5K) | Logically tested on CPU; **not bound to device state** (no `KvPool` use in compute). Keep as design; must be re-wired. | `grep KvPool crates/core/src/compute.rs` → none |
| Bench harness (replay / canary / gate / record proxy) | `crates/bench` (~3K) | Works against mock. Keep. | `record_capture.rs`, `gate_report.rs` |
| Compute adapter (forward pass) | `crates/core/src/compute.rs` (4.4K) | **Does not work; wrong by construction** (§2). Rewrite. | §2 |
| Kernel leaf | `kernel/src` (4.3K) | Compiles; **numerically and structurally not the model's kernels** (§2, §3). Replace. | §3 |
| CUDA graphs | `graph_capture.cu`, `decode_graph_surface.cu` | Captures a non-model sequence. Delete. | §2.7 |
| Specs / ADRs / README | `docs/`, `.scratch/*/specs` | Over-claim ("v1 code-complete", "decode hot path", "faithful port"). Rewrite the status; keep the ADRs that are still true (0002, 0004, 0005, 0006, 0007). | README "Status" |

Test surface: `cargo test --workspace` → 215 passed, 0 failed, **26 ignored**
(every GPU test). The GPU tests additionally self-skip on any
`ComputeError::Kernel(-1)` (ADR 0006 "busy GPU" contract), which is how the
#35 launch bug was invisible for two tickets (`KERNEL-WIP.md` §2).

---

## 2. Correctness defects in the forward pass (all must be fixed for a "sane" output)

Each item is independent; #35 is only the first.

1. **GDN step launch invalid at 27B geometry** (`blockDim.x = 2048 > 1024`)
   — GitHub #35, `gdn_step.cuh`. Known.
2. **NVFP4 scale plane read with the wrong layout.** The artifact stores
   scales in `blockscale-k16-m128x4-v1` (swizzled 512-byte tiles:
   `storage-layouts.md` §4); the kernels index scales as row-major
   `[m][k/16]` (`nvfp4_gemm_decode.cuh:74`, `nvfp4_gemm_prefill.cuh:120`).
   Every NVFP4 GEMM (247 parents, ~85% of the weights) multiplies by the
   wrong group scale. `PENDING.md` (#30 notes) acknowledges this as "later
   re-implementation material" — it is a correctness bug, not perf.
3. **`weight_divisor` not applied.** The dequant is
   `e2m1 × e4m3 / weight_divisor` (`storage-layouts.md` §4, `normalize.rs`
   header: `coeff = e4m3_scale * 1/divisor`); the kernels compute
   `e2m1 × e4m3` only. Weights are off by `2688/amax` per tensor.
4. **GDN gating math wrong.** ninfer: per value head `h`,
   `g = -exp(A_log[h]) * softplus(a[h] + dt_bias[h])`, `beta = sigmoid(b[h])`
   (`ninfer/src/ops/kernel/gdn_gating.cuh:19-28`). ignis: `alpha = exp(a_raw)`
   with `a_raw` = the *first* of 48 a-rows and `beta` = the first b-row,
   shared across all 48 heads (`compute.rs` forward_layers: "the first a /
   first b of the 96-row a_b_projection"); `a_log`/`dt_bias` are loaded and
   unused.
5. **GDN state shape / precision wrong.** Model: 48 value heads × FP32
   `[128][128]` per layer, key head = value head / 3. ignis: one flat bf16
   `[6144][2048]` matrix per layer, a 2048-wide dot mixing all 16 key heads
   (`KERNEL-WIP.md` §3). Structurally a different recurrence.
6. **GDN gated RMSNorm missing.** ninfer applies `gated_rmsnorm` with
   `gdn/norm [128]` (per head) and the z gate before `gdn/output`
   (`text_context_impl.h` gdn_mix). ignis multiplies readout × z only.
7. **RoPE position advanced per layer.** `req_state.seq_pos += 1` inside the
   layer loop (`compute.rs` forward_layers end): token `i` at layer `l`
   rotates at `i*64 + l`, documented as the "pos contract". The model rotates
   every layer at position `i`. Attention over the prompt is scrambled.
8. **GQA output gate ignored.** The fused `query_key_gate_value` has gate rows
   (8192..14336) that sigmoid-gate the attention output; ignis projects and
   drops them (`PENDING.md` #30 fidelity notes). Also q/k norm is per-head
   but `key_norm`/`query_norm` are applied correctly only if the fused-plane
   row order matches — unverified (§3.2 "Fused parent row order" in
   `qwen3.8-27b-artifact.md` is not referenced anywhere in ignis).
9. **No EOS / stop-token handling.** Decode stops only on `max_tokens`
   (`compute.rs:2493-2507`, `3351`). Every completion runs to the cap.
10. **The decode graph path is not the model** (§0.4): batch-1 decode returns
    logits from a 5-kernel toy graph over zero state.

Consequence: the "sane output" floor (ADR 0005/0007) is unreachable on
this path. There is no partial credit: any one of 2, 3, 4, 5, 7 alone makes
the output noise.

---

## 3. Performance structure: why the current path cannot approach the 99% gate

### 3.1 Data movement (the architecture)

- Per request, `RequestState` holds on the **host**: the full paged KV
  (`num_blocks × num_kv_heads × block_size × 256 × 2 planes` bf16), the GDN
  state (48 × 6144 × 2048 bf16 = **1.2 GB**), conv state
  (`compute.rs:1216-1245`).
- `ignis_gqa_attention_decode` takes host pointers and H2Ds the whole KV
  buffer each call (`ignis_kernel.h:28`, "All buffer pointers are host
  memory"). 16 attention layers × per token.
- `ignis_gdn_step` H2Ds/D2Hs the layer's state slice: 48 × 25 MB in + 25 MB
  out per token = **2.4 GB of PCIe traffic per decode token** for GDN alone.
- The GDN readout `S^T k` is a host loop over 6144 × 2048 f32 per layer
  (`state_readout`, `compute.rs:2517`): ~600 M FMAs on the CPU per token.
- Residual add, SiLU×up, final norm loops: host.
- No streams (`stream = null` everywhere), no async copies, no pinned host
  memory, synchronous `cudaMemcpy` inside every leaf call.

Rough floor per decode token on this design: seconds, not milliseconds.
ninfer: 13.1 ms (MTP0). The gap is not a kernel-tuning gap.

### 3.2 Kernel quality (per op, vs the reference)

| Op | ignis | ninfer | Gap |
|---|---|---|---|
| NVFP4 GEMV (decode) | 1 block/row, scalar E2M1/E4M3 decode per element, `switch`-based, byte loads | warp-MMA rowsplit, vectorized 512-element tiles, shared-staged, PDL; BF16 GEMV at 90% of HBM BW | ~10–30× |
| NVFP4 GEMM (prefill) | 16×16 tile, fp32 smem, scalar FMA, bf16 activations | W4A4 tensor-core MMA + TMA at 52–59% of FP4 peak (1676 TFLOP/s); A4 activation quant with site divisors | ~50–100× |
| GQA decode attention | 1 block/head, serial loop over keys, `__syncthreads` block reduce **per key** | tensor-core split-KV, online softmax, bf16/int8/hq KV sources, at roofline @260k | ~100× at long context |
| GQA prefill attention | (not inspected in detail; same scalar style) | bf16/i8 tensor-core causal, 1024-token chunks | large |
| GDN step | 1 thread/column, 1024+ threads, smem reduce | warp-tiled 128-d_k per warp, register state, shuffles, `__launch_bounds__(128,2)` | ~10× + invalid launch |
| GDN prefill | per-token recurrence loop in Rust | chunked kernels (`prepare_wy_wu`, `state_passing`, `output`) on tensor cores | ~100× |
| KV cache | bf16 paged, host-resident | hq-e8-2b (HyperQuant) / int8 / bf16, device paged, exact-key side store | feature gap |
| lm_head | bf16 dequant of W8 (2.5 GB VRAM), bf16 GEMM | W8G32 GEMV streaming 1.35 GB | 2× bytes |
| Launch structure | ~one FFI + 2 memcpy per op | ~630 kernels in one CUDA graph per decode round, PDL chains | — |

### 3.3 Batching

`decode_step` runs the N lane jobs **serially** (`compute.rs:3289-3292`);
`prefill_step` too. ninfer's core invariant is "maximal batched decode: one
model traversal for all B decode-ready requests"
(`concurrent-inference-architecture.md` §2.1). Without batched decode, N=8
lanes divide throughput by 8 instead of amortizing weight reads. The "batched
prefill" of B1 is batching *tokens of one request* (multi-token GEMM), which
ninfer also does (chunked prefill) — it is not the north-star "concurrent
prefill across requests" experiment.

---

## 4. Process / documentation debt (why this happened, what to change)

1. **"Green" is decoupled from "works".** Every GPU test is `#[ignore]` and
   self-skips on any kernel error. Tickets were closed on CPU-green +
   synthetic geometry (`state_cols = 8`, vocab 256). The real geometry was
   never exercised until the last ticket. Rule to adopt: **no kernel/compute
   ticket closes without a GPU-run numeric check at real geometry against an
   oracle** (§6.1). "Skip = green" is retired for compute work.
2. **Provenance claims are inaccurate.** Kernel headers say "faithful
   adaptation … ported 1:1"; the ported part is the E2M1 table. A reviewer
   trusting the header would not open ninfer's file. Rule: a port claim must
   name the source file and be diffable; anything else is "own
   implementation".
3. **Over-claimed status.** README: "v1 is code-complete; the next milestone
   is the 99% gate". PENDING: "the decode graph is now the decode hot path".
   Neither is true. The ledger has become a record of tickets *closed*, not
   of capabilities *working*.
4. **Ticket granularity produced glue, not an engine.** The A1/A2/A3/B1/B2
   split was "make each op callable" then "assemble". Each op was validated
   in isolation with synthetic shapes and host buffers; the assembly inherited
   every simplification. Inference engines are built the other way: a
   device-resident program with one end-to-end oracle from day 1, then op by
   op replaced/optimized under that oracle.
5. **Monolith.** `compute.rs` is 4.4K lines mixing model config, weight
   containers, dequant, per-op FFI wrappers, the forward pass, graph plumbing
   and artifact routing. Split is needed anyway once the forward moves to
   C++.
6. **Docs are consistent with each other but not with the code.** CONTEXT.md
   / design doc describe a target architecture as if built. ADR 0008
   (staging-buffer graphs) solves a problem the real design won't have (the
   graph must carry per-slot KV/state views, batch-width variants, and
   per-step token/position inputs — ninfer's `decode_graph.cpp` model).

---

## 5. Reorganization: what to keep, what to change, what to delete

### 5.1 Keep (with re-wiring)
- `crates/artifact` — the reader/binder/checksum/frontend are the strongest
  asset. Add: a device-side view API (plane pointers + layout descriptor) that
  the C++ program consumes directly; drop the host-side W8 dequant once a W8
  GEMV lands.
- `crates/server` — API, SSE, telemetry. Add EOS/stop, sampling params
  plumbing, request-log JSONL (ninfer's `--request-log-jsonl` is what the
  bench and the user rely on).
- `crates/core` scheduler/admission/host-tier/prefix — keep as the control
  plane, but each abstraction must own a **handle to device state** (page
  ids that are real device pages; a GDN slot id; a pinned-host snapshot
  region). The `Compute` trait is replaced by a step-level device runtime
  handle (§5.3).
- `crates/bench` — keep; it is the gate tool.
- `docs/adr` 0002, 0004, 0005, 0006, 0007 — still valid. 0001 revised
  (ABI granularity). 0003 already superseded. 0008 superseded.

### 5.2 Delete / park
- `compute.rs` forward path (`forward_layers`, `forward_layers_multi`,
  `prefill_*`, `decode*`, `state_readout`, host glue) — replaced, not fixed.
- `kernel/src/decode_graph_surface.cu`, `graph_capture.cu` (toy sequence),
  `hello.cu`/`vector_sum`, every `*_surface.cu` with host-pointer contracts.
- The scalar kernels (`nvfp4_gemm_*.cuh`, `gqa_attention_*.cuh`,
  `gdn_step.cuh`) — replaced by real ports. Their f32 CPU references in tests
  can be reused as oracles for tiny shapes only.
- The parked `gdn-step-dk-tiling` worktree (keeps the dead-end flat ABI).

### 5.3 The new shape (the decision to make)

```
crates/server   HTTP, OpenAI/Responses, streaming, tokenizer/chat template, telemetry
crates/core     scheduler, admission, KV page accounting, prefix index, host tier policy
crates/artifact reader / binder / checksum / device materializer (arena)
crates/runtime  (new, thin)  safe Rust wrapper over the step-level C ABI below
kernel/         C++/CUDA "program" layer + ops + tests (its own CMake test binary)
```

Step-level C ABI (device-resident, opaque handles, no host activations):

```
ignis_model_load(arena_view, config) -> model
ignis_seq_alloc(model, max_ctx) -> seq          // KV pages + GDN slot + conv state, on device
ignis_seq_release / ignis_seq_snapshot(host_pinned) / ignis_seq_restore
ignis_prefill(model, seq, tokens[n], pos0, chunk) // chunked; returns last logits (device) or sampled id
ignis_decode(model, seqs[B], tokens[B]) -> ids[B] // one batched round; CUDA-graph per B
ignis_sample_params(...)                         // greedy / temp / top-p / top-k / penalties
ignis_kv_pages_*                                 // page table ops the core's KvPool drives
ignis_stats(...)                                 // kernel counts, graph hits, VRAM
```

The forward pass (the per-layer op sequence: norm → proj → conv → gating →
recurrence → gated norm → out-proj → residual → norm → gate_up → silu_mul →
down → residual) lives in C++, on streams, with device tensors, captured into
a graph per decode batch width. This is what ninfer's
`text_context_impl.h` + `decode_graph.cpp` + `concurrent_executor.h` do. Rust
orchestrating 630 device-pointer launches per round through FFI is possible
but buys nothing and costs the graph capture ergonomics; the ABI boundary
belongs at the step.

This revises ADR 0001's "Rust owns everything above compute": Rust owns
everything above the **step**, C++ owns the step.

### 5.4 Kernel policy (the second decision)

ADR 0005 says "port proven kernels for now, re-implement later." What was
shipped is neither. Options:

- **(A) Port ninfer's kernels verbatim** — the `.cuh` files, their launch
  helpers, and their storage-layout decoders (`nvfp4_codec.cuh`,
  `nvfp4_gemv.cuh`, `nvfp4_small_t.cuh`, `nvfp4_w4a4_*`,
  `gqa_attention_{decode,prefill}_*`, `recurrent.cuh`, `chunked/*`,
  `causal_conv1d.cuh`, `gdn_gating.cuh`, `rmsnorm.cuh`, `l2norm.cuh`,
  `rope.cuh`, `hq_codec.cuh`, `sampling*.cuh`, `mtp_*`, `dflash2_draft.cuh`).
  Apache-2.0 → allowed with NOTICE (already present). The "dispatch layer is
  C++ state the ABI forbids" argument in the kernel headers was a
  misunderstanding: the dispatch stays *inside* the leaf; only the step ABI
  crosses the boundary.
- **(B) Write our own tensor-core kernels** — months per family to reach
  ninfer's numbers (their W4A4 TMA GEMM alone is a long-tuned piece).

Recommendation: **(A) now**, which is what ADR 0005 already says; (B) later
per family, gated by measurement, as the north-star work. Where ninfer is
at roofline (BF16 GEMV, int8 decode attention, GDN chunked prefill —
`ROADMAP-kernel-perf.md` §0) there is nothing to gain from (B) at all.

---

## 6. Path to a usable engine (≥ ninfer), with hard gates

Every phase ends in a **GPU-measured gate**. No phase is "done" on CPU tests.
Order is by dependency, not by ticket size.

### Phase 0 — Reset the ground truth (≈1 week)
- Rewrite README status, PENDING (open: "no working forward"), CONTEXT.md
  (add: GDN head structure 48V/16K ×128 fp32, hq-e8-2b, chunked prefill,
  batched decode round, decode graph per width), design doc §3 milestones.
- ADR 0009: step-level device-resident ABI (supersedes the per-op host-pointer
  contract; revises 0001). ADR 0010: kernel policy = verbatim port with
  provenance (clarifies 0005). Mark 0008 superseded.
- **Oracle.** Build the per-layer numeric oracle before any kernel work:
  run ninfer (`tools/reference`, `tools/parity`, or a small C++ harness
  linking ninfer's ops) on 3 canary prompts, dump per-layer hidden states +
  final logits for the first 32 tokens to disk. This is the acceptance
  instrument for Phases 1–2.
- **GPU-in-the-loop test rule.** A `gpu` test profile that *fails* (not
  skips) when the GPU is busy; run it explicitly by the human/gate, never in
  the default `cargo test`. Kernel leaf gets its own C++ test executable
  (ninfer `tests/ops` style) so ops are checked without Rust.

### Phase 1 — Correct device-resident forward, batch 1, bf16 KV (≈3–5 weeks)
Gate **G1**: greedy completions on the canary suite are coherent text; per-
layer max-abs error vs the oracle within bf16 tolerance; **teacher-forced**
next-token argmax agrees with the oracle on ≥ 95% of the first 32 positions
per canary — each position fed the oracle's own prefix (ADR 0014). A sanity
floor for gross implementation errors, a self-check and not a parity gate
(ADR 0007).

**Verdict, first attempt (2026-09-07, GitHub #62): not green.** Ran the full gate on a free
RTX 5090. Kernel build green. Op/layer/program tests green:
`gqa_layers_match_f64_reference`, `full_program_prefill_and_greedy_decode_are_deterministic`,
the `cuda_leaf` decode test, `seq_alloc_gpu`'s 3 tests. Canary agreement was
recorded as 52% against the ≥ 95% floor (`rust-hello` 21/21, `math-greedy` 32/32,
`rust-sort` 0/32, `explain-reverse` 0/17 — GitHub #72, divergence at token 0).

**Canary metric superseded (2026-09-07, GitHub #76 / ADR 0014).** That 52% is
the *free-running* figure, now diagnostic only: GitHub #72 established the
divergence at token 0 on both canaries is an exact BF16 logit tie, and a
free-running comparison lets one such flip decorrelate every later position.
Re-measured **teacher-forced** on the same fixture and artifact, thinking
disabled: `rust-hello` 21/21, `rust-sort` 30/32, `math-greedy` 32/32,
`explain-reverse` 16/17 — **99/102 = 97.1%**, clearing the ≥ 95% floor, with
the two tie positions counted as mismatches and nothing waived. Bit-identical
across two runs. The remaining genuine mismatch is `rust-sort` position 23
(ignis token 198 at 20.25, oracle token 25 at 19.25 on ignis's logits): a
diagnostic follow-up, not a G1 blocker.
Server e2e (P1-25, `openai_http_gpu.rs`) is red: 2 of 3 tests fail on
default (concurrent) `cargo test` parallelism with a GPU OOM panic; serialized
(`--test-threads=1`) the same 2 tests instead fail on empty/malformed content
because the requests don't disable `enable_thinking` and the model spends the
whole 32-token budget on the reasoning channel (GitHub #70), while VRAM still
climbs to ~31/32 GB and the run takes 4× longer because the test harness never
tears down a prior test's scheduler/driver task (GitHub #71). Separately,
three GPU tests (`model_load_gpu.rs` / P1-17, `gdn_layer_gpu.rs` / P1-22,
`step_degenerate_gpu.rs` / P1-18) lacked `#[ignore]`, so
`scripts/gpu-profile.ps1`'s exact invocation filtered them out every time —
they had never actually run under the profile (GitHub #73, fixed same night,
commit e5c2c21). Verifying the fix gave first real data on all three: P1-17
and P1-22 pass, but P1-18 fails hard — relative L2 error 1.08 against a
0.0039 tolerance (GitHub #74) — a real, previously-undetected correctness
bug in the embedding→norm→output-head→argmax slice. None of these gaps are
waived; each is its own ticket. Master #36 stayed open on this attempt.

**Verdict, G1 GREEN (2026-09-07 21:18Z, GitHub #62, commit `1f2b98a`).** Full
`scripts/gpu-profile.ps1` on a free RTX 5090 (3.3 / 32.6 GiB used at start,
ninfer stopped per ADR 0006), one run, green end to end:

- **Kernel leaf op tests (CTest):** 31/31 passed, 0 failed, 28.74 s — every
  vendored op at real 27B geometry against the reference's own fp64
  references and tolerances (ADR 0010).
- **f64 layer/program references:** `gqa_layers_match_f64_reference`,
  `gdn_layer_matches_f64_reference`,
  `degenerate_program_matches_f64_reference_for_four_tokens`,
  `gqa_and_gdn_references_cover_two_tokens` — all green. #74's L2 error of
  1.08 on the P1-18 slice is fixed and stays fixed.
- **Reproducibility:** `full_program_prefill_and_greedy_decode_are_deterministic`
  green.
- **Canary floor (ADR 0014):** `teacher_forced_canary_agreement_meets_the_g1_floor`
  green — teacher-forced next-token agreement 99/102 = 97.1% against the
  ≥ 95% floor, ties counted as mismatches, nothing waived.
- **Server e2e (P1-25):** `openai_http_gpu.rs` 4/4 green, including
  `finish_reason: stop` on EOS and the thinking-disabled path — the red
  reported above (#70 / #71 / #75) is resolved.
- **Sequence lifetime:** `seq_alloc_gpu`'s alloc/release/realloc and
  zero-context rejection green; `cuda_leaf` prefill+decode green.
- **16 GPU-gated Rust tests passed, 0 failed, 0 skipped.** Under the profile a
  skip is a hard failure, so zero skips is a real zero.

Every G1 criterion is therefore met: coherent greedy completions on the canary
suite, per-layer output within bf16 tolerance of the f64 reference, ≥ 95%
teacher-forced canary agreement, EOS honored, reproducible across loads.
**Master #36 closes on this.** The one open diagnostic — `rust-sort` position
23 — is recorded in PENDING.md and is not a gate blocker.
- Leaf: device arena/tensor, streams, layer program (`text_context`
  equivalent), sequence state (KV pages, GDN fp32 slot pool
  `[48][48][128][128]`, conv taps).
- Ported ops (decode shapes first): NVFP4 GEMV with the blockscale decoder +
  divisor; BF16 GEMV; W8G32 GEMV (lm_head, embedding gather); rmsnorm;
  per-head q/k norm + RoPE (θ=1e7, verify NeoX vs interleaved against the
  oracle); GQA decode attention (bf16 paged); causal_conv1d+SiLU; gdn_gating;
  GDN recurrent step (per-head, fp32, L2-normed q/k, 1/√128); gated
  rmsnorm; silu_and_mul; residual; argmax.
- Prefill in this phase: per-token recurrence is acceptable *only* to reach
  G1 (it is what makes the oracle comparable); Phase 2 replaces it.
- Rust: `crates/runtime` wrapper; server end-to-end on one request; EOS.

### Phase 2 — Real prefill (≈2–3 weeks)
Gate **G2**: TTFT at 8K / 32K prompts ≤ 1.5× ninfer MTP0 (ninfer: ~8.5K tok/s
@24K → ~2.9 s); oracle still green.
- Chunked prefill (1024) with: W4A4 activation quant (site divisors) + the
  W4A4 MMA/TMA GEMM; `nvfp4_small_t` for small T; bf16 tensor-core prefill
  attention with KV append; GDN chunked kernels (`prepare_wy_wu`,
  `state_passing`, `output`); shared-prefix boundary hook (for Phase 4).

**G2 verdict — PASS (2026-09-09).** Both engines measured live on the free
RTX 5090 against the same artifact
(`F:\ai\q38\ninfer-models\qwen3_8_27b_nvfp4full-v2.ninfer`, v2, checksum
clean), through the same harness (`ignis-bench ttft --corpus`, prompts cut
from `bench_corpus.ids`), cold-prefix cells, five samples each:

| cell | ignis | reference | ratio | verdict |
|------|-------|-----------|-------|---------|
| 8K | 749.6 ms | 853.6 ms | 0.878 | PASS |
| 32K | 3819.7 ms | 4490.3 ms | 0.851 | PASS |

ignis: branch `p2-06-ttft-corpus` on `4d03d40`, leaf rebuilt from `kernel/`
at that tree; BF16 KV, 1024 prefill chunk, eager (no graphs), 40,960-token
context, 65,536-token KV pool. Reference: `ninfer-serve` from
`F:\ai\q38\ninfer\build-ninja` in the owner's production profile — hq-e8-2b
KV, 1024 chunk, CUDA graphs, speculative MTP. Records:
`.scratch/g2-logs/g2-ignis-release-20260909.json`,
`.scratch/g2-logs/g2-reference.json`, verdict
`.scratch/g2-logs/g2-verdict-release-20260909.json`.

The KV-format difference is recorded as a known inequality and not corrected
for. It runs against ignis: ignis carries the more expensive BF16 KV, has no
graph capture and no speculation, and still lands under the reference on both
cells.

**Retired 2026-09-13 (GitHub #143).** hq-e8-2b has since been measured on
*both* sides, which is the one thing this inequality was waiting for: a
live/live hq-both-sides G3 pair on one tree and one card returned C=1 1.054,
C=4 3.635 and ITL p95 0.995 — all three clean, at G3's own thresholds
(`.scratch/g3-live-verdict.json`, 2026-09-13). The KV format is no longer a
standing asymmetry between the two engines, and the ITL gap it was once
offered to explain turned out to be the decode round rather than KV
precision (#110, closed 2026-09-10 — Phase 3 below).

Three things recorded rather than waived:

1. **The legs are not interleaved.** ADR 0015 asks for live/live in one
   session; the reference leg was taken 2026-09-08 23:59 and the ignis leg
   2026-09-09 02:24, each with the GPU exclusively its own, same session id,
   same harness, every sample cold. A margin of 0.85–0.88 against a 1.5
   threshold is far outside what two hours of drift can move, but a strictly
   interleaved re-run is what would close the letter of the ADR.
2. **The first attempt at this gate was invalid.** On 2026-09-09 01:07 ignis
   measured ~150 s on the 8K cell and the run was written up as a ~175x gap
   (#93). That was a stale kernel archive, not the engine:
   `crates/artifact/build.rs` rebuilt the leaf only when `kernel/build/*.lib`
   was *missing*, so the binary under measurement carried a pre-#84 kernel
   that still prefilled a span one token at a time — 18.2 ms/token, the cost
   of a decode traversal (#94). Rebuilt from the same sources, the same
   commit measures the table above.
3. **The first profile run of this gate was red** (`gqa_layer_gpu`, two runs
   in five). Diagnosed and fixed as **#96**: the test issued its H2D on the
   device's load stream and launched the layer on the model's stream with
   nothing ordering them, so the kernel could read the previous token's
   residual. A harness race on a path production does not use — the program's
   own chunk loop copies its ids on `model->stream` itself. #96 also found
   that this oracle had been given `gdn_layer_gpu.rs`'s tolerance (0.32)
   without anyone measuring the GQA layer, which actually runs at ~0.004; that
   80x of slack is what nearly hid the race. Its bound is now 0.006. The
   profile is green in one run on the fixed tree: 32/32 kernel op tests, 25
   Rust GPU tests, zero failures.

### Phase 3 — Serving loop: batched decode + graphs + sampling (≈2–3 weeks)
Gate **G3**: C=1 MTP0 decode ≥ 99% of ninfer's 75–76 tok/s; C=4 aggregate
≥ 99% of ninfer's C=4; p95 inter-token latency while another request
prefills within ninfer's envelope.
- One batched decode round for all decode-ready lanes (ninfer §2.1); CUDA
  graph captured per batch width at startup (eager for uncaptured widths);
  PDL where the ported kernels support it.
- Sampling: greedy, temperature, top-p, top-k, presence penalty; seed.
- Core re-wired: `KvPool` pages are device pages; admission's capacity
  math uses real bytes; `GdnState` boundaries map to real slot checkpoints.
- Telemetry + request-log JSONL with per-request phase rates (the bench's
  input).

**G3 verdict — PARTIAL PASS, one gap filed (2026-09-10).** Both engines
measured live/live in one session (`g3-20260910T001050Z`) on the free RTX
5090 against the same artifact
(`F:\ai\q38\ninfer-models\qwen3_8_27b_nvfp4full-v2.ninfer`), through
`ignis-bench g3` over HTTP/SSE, prompts cut from `bench_corpus.ids`:

| cell | ignis | reference | ratio | verdict |
|------|-------|-----------|-------|---------|
| C=1 (tok/s) | 71.7 | 65.5 | 1.095 | PASS |
| C=4 aggregate (tok/s) | 20.5 | 9.8 | 2.089 | PASS |
| ITL p95 (ms) | 201.14 | 177.93 | 1.130 | **FAIL** (tolerance ≤ 1.1) |

ignis: `BF16 KV, 1024 chunk, CUDA graphs, K=1`, 40,960-token context,
65,536-token KV pool — the serving prefill chunk width used for this run is
**1024**, the same width the reference uses and the width `ignis-server`
reserves program scratch for at load. Reference: `ninfer-serve` in the
owner's production profile — `hq-e8-2b KV, 1024 chunk, CUDA graphs, prefix
reuse`. Records: `.scratch/g3-logs/g3-ignis-chunk1024-20260910.json`,
`.scratch/g3-logs/g3-reference-20260910.json`, verdict
`.scratch/g3-logs/g3-verdict-chunk1024-20260910.json`.

A chunk=512 comparison run in the same session made ITL p95 *worse* (ratio
1.181, `.scratch/g3-logs/g3-verdict-chunk512-20260910.json`) — p50 fell as
chunk-proportional cost predicts, but p95 rose, pointing at a fixed
per-chunk-boundary cost as the tail driver rather than raw chunk-processing
time. The search stopped there rather than becoming an ad hoc parameter
sweep (spec 03 rules out an adaptive chunk-width policy this phase); the gap
is filed as **#110**, not waived.

Also required and green on this tree, same GPU-exclusive sitting:

- The functional anti-serialization property (P3-01 CPU test,
  `crates/core/tests/interleaving.rs`,
  `a_decode_round_accompanies_every_chunk_while_a_lane_is_decode_ready`) —
  independent of the ITL ratio above, per the gate's own design.
- G2 correctness: teacher-forced canary floor (ADR 0014) and the
  chunked-vs-per-token self-oracle, both green.
- GPU profile (ADR 0006): `kernel/build.ps1 -Test` 33/33 kernel op tests;
  `cargo test --workspace --features cuda -- --ignored --test-threads=1`
  all green, zero failures, zero skips.

**Recorded inequality carried over from the ticket, not re-derived here:**
N=8 at long context is not reachable with BF16 KV. A sequence-token costs
64 KiB (16 GQA layers × 4 KV heads × 256 × K+V × 2 bytes), so the
65,536-token pool is ~4 GiB and eight lanes at full context would be ~20 GiB
next to ~19 GB of weights. N=8 in BF16 exists at short contexts; G4's
hq-e8-2b makes it practical at long ones.

#64 closes on this verdict per its own text ("Closes when #104 records the
G3 verdict") — the ITL gap is tracked forward as #110, not held open here.

**#110 closed 2026-09-10.** Re-measured live/live after #111 under ADR 0021
launch pooling — two independent launches per engine, session
`g3-110-113-20260910`, method and records in
`.scratch/g3-gate-110-113-rerun/README.md`. ITL p95 clears the 1.10 ceiling
in all four launch pairings (0.913–1.012), pooled mean ratio **0.960**. The
term that was the whole gap is the decode round, not KV precision: it moved
from 70.2 ms / 4.06× the reference to 18.6 ms / 1.067×. One profile change
matters for reading those numbers — the reference's `--kv-capacity` is
explicit at 65,536 tokens there, matching ignis's fixed pool, where earlier
sessions let it resolve to 327,680.

### Phase 4 — ninfer feature floor (≈3–4 weeks)
Gate **G4**: the bench-03 99% gate on the recorded "1 main + N subagents"
trace, hq-e8-2b on both sides; needle-retrieval sanity at 64K/128K.
- hq-e8-2b KV (`hq_codec.cuh`, hq attention variants, exact-key side store)
  — the profile the user runs.
- Prefix reuse on device (page refcount → real page sharing; shared
  system+tools boundary capture).
- KV-RAM host tier with real pinned-host snapshot/restore (incl. GDN slot,
  conv taps, side store — ninfer's header v2 sections are the checklist of
  what must be captured).
- Tagged lanes (`@main/@agents/@classifier`), preserve-thinking, tool-call
  parsing hardening (ninfer's streaming fixes), warmup/readiness split.

**G4 verdict, run 2 — ONE CELL FAILS, everything else passes (2026-09-13).**
The follow-up session #128's verdict asked for: a fresh trace, four independent
process launches (ADR 0021), hq-e8-2b and a matched 465,984-token / 7,281-page
KV pool on both sides, one GPU-exclusive session (`g4-20260913T163557Z`), one
agent leg on the card. Records and the full method:
`.scratch/g4-run2/README.md`. Every harness gap #128 filed is closed and was
exercised for real here: **#137**, **#138**, **#139**, **#143**, **#144**,
**#146**, **#147**.

| cell | result | detail |
|------|--------|--------|
| per class (main/sub) pooled ratio >= 0.99 | **main PASS 1.264, sub FAIL 0.969, aggregate PASS 1.013** | Now a real measurement on both sides — #137's fix means the thinking channel is counted, and all 44 requests across the four launches returned tokens, so #147's zero-token refusal never fired. `main` 31.1 vs 24.6 tok/s; `sub` 27.0 vs 27.9; `aggregate` 28.0 vs 27.7. The `sub` miss is consistent, not a draw: both launch pairs land the same side (0.975 and 0.962) with ignis's across-launch spread at 2.2% and the reference's at 2.8%. Filed as **#148**, not waived. One diagnostic is degenerate rather than informative: ADR 0021's "across-launch spread exceeds within-launch spread" flag fires on `main` (2.17% vs 0.00%) only because the class holds a single request per launch, so its within-launch spread is 0 by construction — the trace-shape fragility #142 named. Record: `.scratch/g4-run2/g4-verdict.json`. |
| C=1, C=4, ITL p95 (G3 cells, hq both sides) | **PASS — every ratio inside the band** | Pair 1: C=1 0.990, C=4 3.197, ITL p95 0.986. Pair 2: C=1 0.982, C=4 3.836, ITL p95 1.028 (flagged: the reference's window covered 6 of 10 prefill windows, its lane 0 stopping on its own EOS — the case #139/#146 fixed, which now warns and still produces a verdict-ready record where it used to refuse the leg). Spec 04 reads this cell as a regression band at G4 (fails only below 0.90 on throughput or above 1.10 on ITL p95), so `ignis-bench g3-gate`'s own exit code — which applies G3's 0.99 threshold and therefore reports FAIL on C=1 in both pairs — is not this cell's verdict here (#143). Records: `g3-verdict-pair{1,2}.json`. |
| needle retrieval @ 64K / 128K | **PASS, 8 of 8** | Both lengths retrieved on every launch of both engines. At run 1 the 128K cell failed 4 of 4, identically on both engines, on the `ignis-bench` client's undeclared 30 s deadline (**#138**); the harness now declares its own 1,800 s deadline and prints it before the first request. |
| canary self-consistency | **PASS at the serving default** | 4/4 `sane=true deterministic=true`, `self-consistency: PASS`, with thinking **on** — run 1 needed `--enable-thinking false` to pass. Three canaries report `answer=thinking-only`, which is #144's fix saying so rather than leaving an empty field to be misread; the record carries both channels (`first_reasoning` is coherent and identical across the two runs). Record: `ignis-launch1-canary.json`. |
| dogfood — real agent task, intact tool-call/thinking streams | **PASS** | One real tool-calling turn against ignis launch 2, inspected as raw SSE: 31 data frames, zero malformed, 28 clean `reasoning_content` chunks, a well-formed `tool_calls` delta whose arguments parse (`{"path": "crates/bench/src/g4_gate.rs"}`), `finish_reason: "tool_calls"`, terminal `[DONE]`. Record: `dogfood-raw-sse.txt`. |
| GPU profile (ADR 0006) | **PASS, clean on the first attempt** | `scripts/gpu-profile.ps1` end to end, exit 0: `kernel/build.ps1 -Test` **40/40**, then the serialized `--ignored` Rust sweep under `IGNIS_GPU_PROFILE=1` **39 passed, 0 failed, 0 skipped** (72.2 s kernel + 3.8 s f64 oracle + 635.3 s GPU tests = 711.4 s). Run 1 needed four attempts for one clean sweep (**#145**); this session met the condition #145's finding identified as the real one — a single agent leg with the whole card, no second GPU-touching process. Log: `.scratch/g4-run2/gpu-profile.log` (gitignored; the stage summary is above). |

**The KV-format inequality is retired twice over.** The gate's own G3 cells ran
hq on both sides, and a separate live/live 2x2 over engine and format — eight
legs, two launches per cell, one session, matched 65,536-token capacity —
measured the format's own cost at **-14.5% of ITL p95 for ignis and -14.3% for
the reference** going hq to BF16. Two independently written engines paying the
same toll to a tenth of a point is the format's cost, not ignis's. The same 2x2
puts ignis at C=1 1.056 / C=4 3.074 / ITL p95 1.031 under hq and 1.015 / 1.941 /
1.030 under BF16 — so ignis's one deficit, the ITL tail, is the *same* 1.03 in
both formats and therefore not a codec problem.
Finding: `docs/findings/2026-09-13-hq-vs-bf16-live-live.md`; records
`.scratch/g4-run2/kvab/`.

**What is still open.** **#148** (`sub` pooled ratio 0.969). Everything else
this gate measures passed, and no cell was waived.

**G4 verdict, run 1 — GAPS FILED, not a clean pass (2026-09-12).** Kept as
the record of how the gate's own instrument was found broken; every cell in
it is superseded by run 2 above. Both engines
built and measured hq-e8-2b on both sides, live/live, one GPU-exclusive
session (`g4-20260912T162610Z`) against
`F:\ai\q38\ninfer-models\qwen3_8_27b_nvfp4full-v2.ninfer`, over **two
independent process launches per engine** (ADR 0021). A fresh "1 main +
10 subagents" trace was recorded this session (`bench/traces/g4-load-trace.meta.json`,
hash `51d44a789e...`) since #118's original trace file was never committed
and is no longer on disk. #117-#127 (the phase's own feature work) are all
closed and their machinery is real: hq-e8-2b, state snapshot/restore,
device-side prefix reuse, tagged lanes and the g4/g4-gate instrument all
exist and were exercised. (This session ran as two overlapping agent legs
against the same worktree without initial coordination; the numbers below
are merged from both and cross-checked against each other rather than
picking one account.)

| cell | result | detail |
|------|--------|--------|
| per class (main/sub) pooled ratio ≥ 0.99 | **UNVERIFIED** | `ignis-bench`'s SSE reader (`crates/bench/src/client.rs::read_sse`) only counts `delta.content`, never `delta.reasoning_content` — under this trace's thinking-enabled agentic traffic most/all tokens land on the ignored channel. `ignis-bench g4-gate` *does* run to a real verdict over 2 ignis + 2 reference launches (not refused — both engines now have 2 complete G4 launches on record) and reports `sub`/`aggregate` PASS, `main` FAIL (ratio 0.000) — but none of those three numbers should be trusted as real throughput while this bug stands; `main`'s FAIL is the most visibly broken (3 of 4 launch readings are exactly 0.0 tok/s) but `sub`/`aggregate`'s PASS is built on the same broken counter, not independently verified. Filed as **#137**; a trace-shape gap (the trace carries only one `main`-class request, making that cell fragile regardless) filed as **#142**, closed as superseded by #137 once the deeper cause was confirmed. Record: `.scratch/g4-logs/g4-verdict.json`. |
| C=1, C=4, ITL p95 (G3 cells, hq both sides) | **PASS, re-read against the band (2026-09-13)** | Read at G3's 0.99 threshold this cell recorded PARTIAL out of the `g4-20260912T162610Z` session: launch1 C=1 0.974 **FAIL**, C=4 2.706 PASS, ITL p95 1.030 tolerated-not-clean (`.scratch/g4-logs/g3-verdict-launch1.json`), and the launch2 pair unscorable because the reference never produced a cold ITL sample in three attempts — the same decode lane hit its own EOS before the measurement boundary every time (2,856 / 2,669 / 2,746 tokens), so `ignis-bench g3-gate` refused to compute a ratio (ignis's own launch2 was clean, `.scratch/g4-logs/ignis-g3-launch2.json`). **Both readings are superseded.** #139 (the fixture refusal) is fixed and **closed**: the reference's ITL cell now produces a verdict-ready record even when its lane 0 stops at its own EOS. And the 0.99 threshold does not belong on this cell at G4 — spec `.scratch/runtime/specs/04-reference-feature-floor.md` now reads it as a **regression band**: every ratio reported, the cell fails only below 0.90 on throughput or above 1.10 on ITL p95, and a ratio inside the band but away from parity is recorded as a finding rather than waived. The threshold sat inside the cell's own noise — two live/live hq-both-sides pairs measured ten minutes apart on one tree and one card returned C=1 **1.054** and **0.975** (`.scratch/g3-live-verdict.json`, `.scratch/g3-partial-verdict.json`, 2026-09-13), while ADR 0021 already records 5–17% between launches of one binary; a 0.99 floor tolerates 1%, so at that width the cell decides which launch was drawn, not which engine is faster. Every ratio above is inside the band. The clean pair — C=1 1.054, C=4 3.635, ITL p95 0.995, passing even at G3's own thresholds — is the first hq-both-sides measurement that clears every cell, and it is what retires the BF16-vs-hq KV inequality standing since G2 (Phase 2 above). Filed as **#143**, closed on this reading. One gap remains and is not this cell's: `g3-gate` takes one record per side and cannot pool launches the way `g4-gate` does (`crates/bench/src/main.rs:813`) — that belongs to #116 / ADR 0021, and is not worth building for a cell that is now a band. |
| needle retrieval @ 64K / 128K | **FAIL @128K, confirmed two ways** | 64K **RETRIEVED** on every attempt, both engines, every launch. 128K **FAILED** deterministically on *every* attempt (2 ignis launches, 2 reference launches — 4/4), identically: `read SSE: error decoding response body`. Reference server logs show the engine still mid-prefill (~30s in) when the client gives up; raising ignis's own `--request-timeout` from 30s to **900s** (both ignis launches used this) made no difference, ruling out that flag as the cause. Same failure against two independently-built engines (ninfer's C++/CUDA stack, ignis's Rust/CUDA stack) points at the shared `ignis-bench` client, not either engine. Filed as **#138** (canonical; a near-duplicate filed independently as #141 was closed in favor of it once the overlap was spotted). |
| canary self-consistency | **PASS** (with a caveat) | Fails under the server's thinking-on default: 3 of 4 canaries return genuinely empty output (`"first": "", "second": ""`, deterministic) because canary's fixed 64-token budget (`crates/bench/src/canary.rs:142`) is entirely consumed by reasoning before any content token is produced — related to, but a distinguishable mechanism from, #137's content-only blind spot (filed as **#144**, cross-referenced with #137). Re-run against `ignis-server --enable-thinking false`: all 4 canaries `sane=true deterministic=true`, `self-consistency: PASS` (`.scratch/g4-logs/ignis-canary-nothink.json`). |
| dogfood — real agent task, intact tool-call/thinking streams | **PASS** | A real tool-calling request against `ignis-server` (thinking on) produced a clean `reasoning_content` stream, a well-formed `tool_calls` delta with valid JSON arguments, `finish_reason: "tool_calls"`, terminal `[DONE]` (`.scratch/g4-logs/dogfood-raw-sse.txt`). This is also the evidence that pinpoints #137's root cause: the turn has zero `delta.content` chunks by design. |
| GPU profile (ADR 0006) | **PASS, but flaky across attempts** | `kernel/build.ps1 -Test` 40/40 (`kernel/build/Testing/Temporary/LastTest.log`). The Rust `--ignored` sweep needed **4 attempts** to get one clean run: one attempt was corrupted by an operator error (two concurrent `gpu-profile.ps1` processes launched by the two overlapping agent legs, killed and restarted); after that was cleanly resolved (confirmed via `nvidia-smi`/`tasklist`, GPU at idle baseline, single process), it *still* crashed twice more — once in `openai_http_gpu.rs`'s multi-thousand-token streaming test, once in `chunk_decomposition_gpu.rs` (which hardcodes BF16, ruling out an hq-specific cause) — both times an abrupt process exit with no panic text despite `--nocapture`, both individually reproducible-clean in isolation. The fourth attempt passed 0 failures and is the run this verdict relies on, but the recurring flakiness across unrelated tests looks environmental (Windows/WDDM GPU-memory pressure over a long serialized run), not resolved by "it was one operator mistake." Filed as **#145**. |

**Per spec: any gap is filed, never waived.** #137 (bench harness ignores
the reasoning channel — the primary blocker), #138 (needle@128K client-side
SSE disconnect, not explained by any server-side timeout), #144 (canary
budget vs. thinking) and #145 (GPU profile flakiness beyond the one
operator-error incident) are filed with full evidence and reproduction
records under `.scratch/g4-logs/`. Two more were filed from this session and
are **already closed**: #139 (G3 corpus fixture lane-0 EOS blocking ADR-0021
pooling on the reference side) and #143 (the G3-under-hq C=1 and ITL reading
— resolved by making that cell a regression band, row above). None of them
implicate #117-#127's actual feature work — hq-e8-2b, state transfer, device
prefix reuse and tagged lanes all built, loaded and served correctly across
every launch. What remains unverified is whether the engine is *fast enough*
and *fully correct at 128K*, because the measurement instrument built for G4
(#117) has its own real bugs. #65 stayed open pending a follow-up
measurement session once #137/#138 landed — re-running the gate with a harness
that can see the numbers it is trying to measure. **That session is run 2
above** (2026-09-13): every gap this run filed is closed, and the gate now
reaches a verdict on real numbers, failing one cell (#148).

### Phase 5 — Speculative decoding (≈3–4 weeks)
Gate **G5**: ≥ 99% of ninfer's MTP7-adaptive / DFlash2-7 committed tok/s at
24K / 98K / 196K (150 / 104–132 / 79–144 tok/s per `SEPT-OPTIMIZAZION.md`).
- MTP first (single MTP layer already in the artifact; `mtp_round`,
  `mtp_pack`, adaptive width controller, ReplaySSM records for GDN rollback).
- DFlash2 second (5-layer drafter module load from the v2 artifact,
  `dflash2_draft.cuh`, RAM-tier KV carry for the drafter).
- Vision is *not* on the floor for a coding engine; defer past G5.

**Re-cut 2026-09-13 (the G5 grilling session, #66).** The order above is
reversed: the DFlash2 drafter ships and MTP is deferred, because the
reference's own depth discriminator puts DFlash2 level at 24K and +27% /
+82% ahead at 98K / 196K, where agentic prompts live, and most of the work
is the drafter-agnostic verify substrate either way. The gate's reference
lane is therefore DFlash2-7 alone, measured live/live (the SEPT figures
above are sizing, never the reference side of a cell), pooled over two
launches (ADR 0021), **run once at the end of the phase** — per-ticket
checks are op tests and the greedy spec-on == spec-off equivalence, which
is the phase's correctness floor. The drafter's per-sequence state (its
2048-token window and checkpoint, 80 MiB) is carried in the snapshot rather
than rebuilt on restore; the numbers behind that and the trigger to re-open
it are in `.scratch/DEFERRED-DECISIONS.md` (G5 items 1–4). Spec:
`.scratch/runtime/specs/05-speculative-decoding.md`; tickets #149–#156
(ROADMAP, "Phase 5 decomposition"). #65 closed the same day on its run-2
verdict with the `sub` cell (#148) accepted as an optimization gap.

### Phase 6 — Beyond ninfer (the north star)
Only after G4/G5: concurrent prefill across requests / prefill-decode overlap
(ninfer explicitly does not do this — the real differentiator for subagent
bursts), fusion + PDL everywhere (`ROADMAP-kernel-perf.md` WI-K2: −1 ms/step),
lazy graph capture, hot reload, our own NVFP4 artifact recipe, lane QoS.

Rough total to G5: **14–20 weeks** of focused work. To G3 (a usable
single-user local engine at ninfer MTP0 speed): **7–11 weeks**.

---

## 7. Decisions needed from the owner

1. **ABI level:** step-level device-resident C ABI with the forward in C++
   (recommended) vs Rust-orchestrated device ops. → ADR 0009.
2. **Kernel policy:** verbatim port of ninfer ops with provenance
   (recommended, Apache-2.0, NOTICE exists) vs own kernels first. → ADR 0010.
3. **Reset scope:** delete the compute.rs forward + toy graphs now (recommended;
   they mislead every future reader) vs keep behind a feature flag.
4. **Oracle source:** ninfer per-layer dumps (recommended; same artifact,
   same numerics) vs an HF/PyTorch reference (needs a second GPU-sized
   environment; not available on this box).
5. **First KV dtype:** bf16 for G1–G3, hq-e8-2b at G4 (recommended) vs hq
   from the start.
6. **Process:** GPU gate per compute ticket with a named oracle check; retire
   "skip = green" for compute; kernel leaf gets its own test binary. Agent
   tickets are cut per *op family under the oracle*, not per "surface".

---

## 8. Appendix — reference numbers (ninfer, RTX 5090, same artifact)

| Metric | Value | Source |
|---|---|---|
| Weight bytes streamed per decode token | 15.85 GB → 9.47 ms floor @1674 GB/s | `ROADMAP-kernel-perf.md` §2.1 |
| MTP0 decode, C=1 | 75.2–76.5 tok/s (13.1–13.3 ms/step) | HANDOFF §5 |
| Kernels per decode step | ~630 (533 graph nodes after fusion) | SEPT §2, HANDOFF |
| Prefill @24K / 98K / 196K | 8.5K / 4.5K / 2.8K tok/s | SEPT §1 table |
| MTP7-adaptive decode @24K / 98K / 196K | 150 / 104 / 79 tok/s | SEPT §1 |
| DFlash2-7 decode @24K / 98K / 196K | 147 / 132 / 144 tok/s | SEPT §1 |
| NVFP4 W4A4 GEMM T=1024 | 52.7–58.8% of FP4 peak | linear-benchmark §9.12 |
| BF16 GEMV | 90–92% of sustained HBM read | linear-benchmark §9.7 |
| GDN state per sequence | 48 layers × 48 heads × 128² × 4 B = 144 MiB fp32 | replayssm-gdn.md §1.1 |
| Model | 64 layers (16 GQA + 48 GDN), hidden 5120, inter 17408, 24q/4kv ×256, GDN 16K×128 / 48V×128, vocab 248320 | qwen3.8-27b-artifact.md §2 |
